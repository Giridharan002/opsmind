response = Map();

// newuser is a boolean which defines whether the user is a first time subscriber or not!
if(newuser)
{
	card = Map();
	card.put("title","OpsMind - AI Operations Intelligence");
	card.put("icon","https://i.imgur.com/OpsMind.png");
	card.put("theme","modern-inline");
	response.put("card",card);
	
	slidesList = list();
	slidesList0 = Map();
	slidesList0.put("type","images");
	dataList = list();
	dataList.add("https://i.imgur.com/OpsMind_Demo.png");
	slidesList0.put("data",dataList);
	slidesList.add(slidesList0);
	response.put("slides",slidesList);
	
	response.put("text","🎉 Hey *" + user.get("first_name") + "*! Welcome to **OpsMind**!\n\n🤖 I'm your AI operations analyst. I can help you:\n\n✅ **Monitor** team operations in real-time\n⚠️ **Detect** tasks at risk\n📊 **Analyze** workload overload\n🔥 **Track** incident patterns\n🚨 **Identify** why sprints are delayed\n\nUse `/ops help` to see all available commands or check the **OpsMind Widget** for a visual dashboard!");
}
else
{
	card = Map();
	card.put("title","OpsMind - AI Operations Intelligence");
	card.put("icon","https://i.imgur.com/OpsMind.png");
	card.put("theme","modern-inline");
	response.put("card",card);
	
	slidesList = list();
	slidesList0 = Map();
	slidesList0.put("type","images");
	dataList = list();
	dataList.add("https://i.imgur.com/OpsMind_Demo.png");
	slidesList0.put("data",dataList);
	slidesList.add(slidesList0);
	response.put("slides",slidesList);
	
	response.put("text","👋 Welcome back, *" + user.get("first_name") + "*! \n\nGlad to have you again! Use `/ops help` to see what's new! 🚀");
}

return response;
